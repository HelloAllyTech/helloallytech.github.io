source "https://rubygems.org"

# Use the official GitHub Pages dependency bundle to match the production environment
gem "github-pages", group: :jekyll_plugins

# Required for local development when running on Ruby 3+
gem "webrick", "~> 1.8"
